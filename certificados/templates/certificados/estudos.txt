📑 SIMULADO DE ENGENHARIA DE SOFTWARE - PROJETO CERTIFICADOS
Aluno: Vitor Luís Fernandes
Data: 04/03/2026

INSTRUÇÕES:
Analise cada questão com calma durante o seu turno.

Marque com um (X) a alternativa que você considera correta.

Não se preocupe com erros; o objetivo é fixar o aprendizado da UniCesumar.

Questão 1: No padrão MVT do Django, qual arquivo é responsável por buscar os dados do aluno no banco de dados e enviá-los para a tela?
( ) models.py (Define apenas a estrutura da tabela).
( ) urls.py (Cuida apenas do caminho/endereço do site).
( ) views.py (Funciona como o "cérebro" que processa a lógica).

Questão 2: Por que usamos o UUID (aquele código longo no link) em vez de um número simples como 1, 2 ou 3?
( ) Para deixar o link mais bonito e colorido.
( ) Por segurança, evitando que alguém tente "adivinhar" o link de outros certificados mudando apenas o número final.
( ) Porque o Python não aceita números inteiros em links.

Questão 3: O que acontece tecnicamente quando usamos a biblioteca qrcode com Base64 no projeto?
( ) A imagem é salva permanentemente na pasta de fotos do computador.
( ) O sistema cria um arquivo físico toda vez que alguém acessa o site.
( ) A imagem é gerada na memória e convertida em texto (código), sendo desenhada diretamente pelo navegador sem ocupar espaço no HD.

Questão 4: Ao tentar acessar o sistema pelo celular usando o endereço 127.0.0.1:8000, por que ocorre um erro de conexão?
( ) Porque o 127.0.0.1 é um endereço local (localhost) que aponta para o próprio aparelho; o celular tenta procurar o site dentro dele mesmo.
( ) Porque o celular não tem o Python instalado.
( ) Porque o Wi-Fi do celular é sempre mais lento que o do computador.

Questão 5: No arquivo HTML, qual a função da tag <style> que usamos hoje?
( ) Definir as frases e o texto que o aluno vai ler.
( ) Aplicar o CSS para dar cor, bordas e organizar o visual do certificado (design).
( ) Criar a conexão com o banco de dados SQL.